Task: Turn town pulse data into one subtle local advantage for a small business.

Input JSON:
{
  "brand": { ... },
  "townPulse": {
    "busyWindows": [ { "dow": number, "hour": number } ],
    "slowWindows": [ { "dow": number, "hour": number } ],
    "eventEnergy": "low|medium|high",
    "seasonalNotes": "string"
  }
}

Output JSON schema:
{
  "angle": string,
  "captionAddOn": string,
  "timingHint": string
}

Rules:
- Keep it simple, local, and natural.
- No analytics jargon.
- No references to data sources.
- No business comparisons or endorsements.
- Use the town rhythm as a subtle edge, not a hard sell.
